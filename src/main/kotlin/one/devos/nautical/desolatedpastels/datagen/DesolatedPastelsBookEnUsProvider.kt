package one.devos.nautical.desolatedpastels.datagen

import com.klikli_dev.modonomicon.api.datagen.AbstractModonomiconLanguageProvider
import com.klikli_dev.modonomicon.api.datagen.ModonomiconLanguageProvider
import net.minecraft.data.PackOutput

class DesolatedPastelsBookEnUsProvider(output: PackOutput?, modId: String?, locale: String?, cachedProvider: ModonomiconLanguageProvider?) :
    AbstractModonomiconLanguageProvider(output, modId, locale, cachedProvider) {
    override fun addTranslations() {

    }
}